<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="boton_azul" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/blanco</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
        <item name="android:textSize">20dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
        <item name="android:background">@drawable/selector_btn_azul</item>
    </style>
    
    <style name="boton_naranja" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/blanco</item>
        <item name="android:shadowDx">2</item>
        <item name="android:shadowDy">2</item>
		<item name="android:textSize">20dp</item>      
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:background">@drawable/selector_btn_naranja</item>
    </style>
    
    <style  name="texto_info" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/blanco</item>
        <item name="android:textSize">20dp</item>
    </style>
    <style  name="texto_fecha" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/azul_boton_claro</item>
        <item name="android:textSize">15dp</item>
    </style>
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>
